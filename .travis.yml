language: node_js
node_js: stable

install:
  - npm install

script:
  - hexo clean
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "vonvww"
  - git config user.email "ww.gggxwy@163.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://$vonvww@$GH_REF" dev:master

branches:
  only:
    - dev
env:
 global:
   - GH_REF: github.com/vonvww/oopstorm.github.io.git
